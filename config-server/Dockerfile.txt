FROM eclipse-temurin:17-jdk

WORKDIR /app

COPY . .

RUN chmod +x mvnw && ./mvnw clean package -DskipTests

# Usa directamente el JAR generado por Spring Boot
CMD ["java", "-jar", "target/config-server-0.0.1-SNAPSHOT.jar"]

